{
  "name": "edf-design",
  "version": "1.0.0",
  "private": true,
  "description": "Alternative interne aux outils de design IA — génération d'UI via le portail IAG EDF, en non-stream",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
